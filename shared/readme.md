
# Kafka in Shared

    I write the the kafka in the shared folder because I will need the same kafka configuration in whole application

    1. My ride backend use it to publish event
    2. Notification workers uses it to consume events
    3. Topic setup scripts uses it to create topics


    

## Shared

    We use shared when we want that different service use the same configuration like kafka