
// Implement KD tree for searching the nearest rider within the radius of the passenger

// px, py are location of the passenger

class Node {
    
    constructor(x, y) {
        this.x = x;
        this.y = y;
        this.left = null;
        this.right = null;
    }
}

const location = (px, py) => {
    
}

const  searchRider(root, )
